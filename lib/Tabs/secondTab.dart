import 'package:flutter/material.dart';

class Secondtab extends StatelessWidget {
  const Secondtab({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container(child: Center(child: Text("2nd Tab"))));
  }
}
