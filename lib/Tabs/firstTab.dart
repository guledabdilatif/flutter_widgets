import 'package:flutter/material.dart';

class Firsttab extends StatelessWidget {
  const Firsttab({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container(child: Center(child: Text("1st Tab"))));
  }
}
