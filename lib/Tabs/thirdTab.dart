import 'package:flutter/material.dart';

class Thirdtab extends StatelessWidget {
  const Thirdtab({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container(child: Center(child: Text("3rd Tab"))));
  }
}
